// import {createSelector} from 'reselect';
// import {RootState} from '../index';

// export const getService = createSelector(
//     (state: RootState) => state,
//     state => state.queryListService,
// );
